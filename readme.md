HTML Boilerplate Package with Gulp and Babel
============================================

Installation
------------
```zsh
git clone git@github.com:elioschmutz/html-boilerplate.git
cd html-boilerplate
npm install
grunt
```

Usage
-----
Just modify the files in ./lib to rebuild your changes.
